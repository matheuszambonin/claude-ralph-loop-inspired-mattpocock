# Estado de execução do próprio Ralph, quando ele roda sobre si mesmo. O
# diretório inteiro: `ralph init` também gera config.json, prompt.md e setup.sh
# aqui, e esses são da máquina que roda o loop, não do projeto — a fonte deles
# mora em templates/.
.ralph/

# Node
node_modules/
npm-debug.log*

# Sistema
.DS_Store
Thumbs.db
